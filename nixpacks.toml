[phases.setup]
cmds = [
    "curl -fsSL https://deb.nodesource.com/setup_18.x | bash -",
    "apt-get install -y nodejs"
]

[phases.install]
cmds = ["npm install --loglevel verbose"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start:prod"